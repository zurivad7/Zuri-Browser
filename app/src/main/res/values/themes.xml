<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Plain platform theme; the real UI is drawn by Compose (ZuriTheme). -->
    <style name="Theme.ZuriBrowser" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
